export interface IconProps {
  icon: string,
  ariaHidden?: boolean,
  className?: string,
  color?: string,
  focusable?: string,
  label?: string,
  title?: string,
  wrapper?: boolean,
  wrapperClass?: string,
  wrapperType?: string
}
