import Link from 'next/link';

// import { useRouter } from 'next/router';

function Navigation() {
  // const router = useRouter();

  return (
    <nav className="page__top">
      <ul>
        <li>seti ui</li>
      </ul>
    </nav>
  );
}

export default Navigation;
